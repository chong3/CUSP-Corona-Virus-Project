	.EQU KEYBOARD_STATUS, $000
	.EQU KEYBOARD_DATA, $001
	.EQU @, $000
	LDX# $001
	
LOOP:
	INB KEYBOARD_STATUS
	JGE LOOP
	INB KEYBOARD_DATA
	STA+ $D00
	CMX# $009
	JGE CHECK
	ADX# $001
	JMP LOOP
	
CHECK:
	